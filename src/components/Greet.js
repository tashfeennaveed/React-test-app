import React from 'react'

 export const Greet=(props) => {
    const {name,hero} = props
    return (
        <div>
            <h1> HELLOW ("_") {name} aka {hero} </h1>
            
        </div>

    ) 
 }

